package com.OndaByte.GestionComercio.Control;
import spark.Spark;

public class UsuarioControl {
    static Route login = (req, res) -> {
        String json = req.queryParams("objeto");

        
    }
}